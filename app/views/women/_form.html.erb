<%= form_for(@woman) do |f| %>
  <% if @woman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@woman.errors.count, "error") %> prohibited this woman from being saved:</h2>

      <ul>
      <% @woman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Nome" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :area, "Área" %><br>
    <%= f.text_field :area %>
  </div>
  <div class="field">
    <%= f.label :current_position, "Cargo atual" %><br>
    <%= f.text_field :current_position %>
  </div>
  <div class="field">
    <%= f.label :linkedin, "Endereço para LinkedIn" %><br>
    <%= f.text_field :linkedin %>
  </div>
  <div class="actions">
    <%= f.submit "Indicar", class: "btn btn-default" %>
  </div>
<% end %>
